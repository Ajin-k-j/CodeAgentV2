# General Settings
PROJECT_NAME="Hybris AI Agent"
VERSION="1.0.0"

# Google Gemini API Key (Required)
GEMINI_API_KEY=your_gemini_api_key_here

# Firebase Credentials
# Option 1: Path to service account file (Recommended for local dev)
FIREBASE_SERVICE_ACCOUNT_PATH=service_account.json

# Option 2: JSON string of credentials (Recommended for Cloud Hosting like Render)
# FIREBASE_CREDENTIALS_JSON='{"type": "service_account", ...}'
# Note: If FIREBASE_CREDENTIALS_JSON is set, it takes precedence over the path.
